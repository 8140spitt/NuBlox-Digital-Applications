name: F05 final formatter

on:
  push:
    branches:
      - feat/f05-product-service-innovation

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/f05-product-service-innovation
      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: app/pnpm-lock.yaml
      - name: Install dependencies
        working-directory: app
        run: pnpm install --frozen-lockfile
      - name: Format lifecycle service
        working-directory: app
        run: pnpm exec prettier --write src/lib/server/product-service/product-service-lifecycle-service.ts
      - name: Commit formatter output and remove helper
        run: |
          rm .github/workflows/f05-final-format.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/lib/server/product-service/product-service-lifecycle-service.ts .github/workflows/f05-final-format.yml
          git commit -m 'F05: format final lifecycle service'
          git push origin HEAD:feat/f05-product-service-innovation
